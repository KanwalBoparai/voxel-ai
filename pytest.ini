[pytest]
testpaths = tests
asyncio_mode = auto
# smoke_test.py is a standalone script (python tests/smoke_test.py); it runs its
# own checks under __main__ and isn't structured for collection.
addopts = --ignore=tests/smoke_test.py -q
